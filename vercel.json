{ 
  "version": 2, 
  "builds": [{ 
    "src": "**/*", 
    "use": "@vercel/static-build", 
    "config": { "rootDirectory": "." } 
  }], 
  "outputDirectory": "public", 
  "routes": [{ 
    "src": "/", 
    "dest": "/index.html" 
  }] 
}
